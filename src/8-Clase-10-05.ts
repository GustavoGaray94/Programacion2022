/*function dibujarGuion() {
  let linea: string = "-";
  for (let indice = 1; indice <= 40; indice++) {
    linea = linea + "-";
  }
  console.log(linea);
}

let num1: number = Number(prompt("Ingrese el 1er número: "));
let num2: number = Number(prompt("Ingrese el 2do número: "));
let option: number = Number(
  prompt("Ingrese 1 para la suma, 2 para resta, otro caracter dará error: ")
);

if (option === 1) {
  dibujarGuion();
  console.log("La suma entre los numeros es " + (num1 + num2));
  dibujarGuion();
} else if (option === 2) {
  dibujarGuion();
  console.log("La resta entre los numeros es " + (num1 - num2));
  dibujarGuion();
} else {
  console.log("Erro, dato opción ingresada incorrecta. ");
}
*/

/*Realice un programa que devuelva la potencia de un número. 
La base y el exponente deben ser ingresados por teclado. 
Sólo deben admitirse exponentes mayores o iguales a cero. 
Recuerde que el resultado de un numero elevado a 0 es 1.
Separe la lógica de calcular la potencia utilizando métodos. */

function potencia() {}

let base: number = Number(prompt("Ingrese la base: "));
let exponente: number = Number(prompt("Ingrese el exponente: "));
